#include <stdio.h>

int main()
{
    int count;
    for (count = 1; count <= 10; count++)
    {
      
            // printf("%d\n", count);
        
            if(count==1){
               
                printf("%d\n",count);
            }
   
    }

    // printf("%d",count);
    return 0;
}